<UserControl x:Class="UzonMailDesktop.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:UzonMailDesktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:StartupViewModel}">
    <Grid Background="#F5F6F8">
        <Border Width="560" Padding="28" Background="White" BorderBrush="#D9DDE3" BorderThickness="1" CornerRadius="6"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="启动检查" FontSize="22" FontWeight="SemiBold" Foreground="#20242A" />
                <TextBlock Text="{Binding StatusMessage}" Margin="0,10,0,16" TextWrapping="Wrap" Foreground="#555D68" />
                <ItemsControl ItemsSource="{Binding MissingPrerequisites}" Margin="0,0,0,14">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Margin="0,4">
                                <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" Foreground="#C2413B" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding Name}" Foreground="#30353B" />
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ProgressBar Height="6" Minimum="0" Maximum="100" Value="{Binding ProgressValue}" Margin="0,0,0,18" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="重新检测" Command="{Binding RetryCommand}" Margin="0,0,8,0" MinWidth="88" />
                    <Button Content="打开下载页" Command="{Binding OpenDownloadPageCommand}" Margin="0,0,8,0" MinWidth="96" />
                    <Button Content="安装并继续" Command="{Binding InstallCommand}" Margin="0,0,8,0" MinWidth="104" />
                    <Button Content="退出" Command="{Binding ExitCommand}" MinWidth="72" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
