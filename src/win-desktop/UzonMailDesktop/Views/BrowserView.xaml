<UserControl x:Class="UzonMailDesktop.Views.BrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid>
        <wv2:WebView2 Source="{Binding Url}" CoreWebView2InitializationCompleted="OnInitializationCompleted" />
        <Border Background="#FFF4F2" Padding="12" VerticalAlignment="Top"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#A33A32" TextWrapping="Wrap" />
        </Border>
    </Grid>
</UserControl>
